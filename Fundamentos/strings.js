'use strict';

let empresa = "servi entrega";
let anios = 42;
let tipo = "mensajeria";
let empleados = 15000;
let origen = "ecuatoriano";
const creacion = 1928;

console.log(`Hoy hablaremos de ${empresa.toUpperCase()}, esta es una empresa que tiene más de ${anios}, que su tipo de trabajo es la 
${tipo} y cuenta con más de ${empleados}, con un origen ${origen.replace("ecuatoriano", "colombiano")} desde el año ${creacion}.`);

